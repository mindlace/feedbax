#include <metal_stdlib>
using namespace metal;
// Placeholder kernel, compiled from source at runtime by MetalContext (Engine/MetalContext.swift).
kernel void fbx_noop(uint2 gid [[thread_position_in_grid]]) {}
